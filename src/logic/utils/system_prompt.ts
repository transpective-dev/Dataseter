import {schema_base} from './core/session/session.interface';

export class Prompts {

    // extract data from text
    public static data_extraction(_schema_base: schema_base) {
        return `

        `
    }
    
    // extract contents from pictures
    public static content_extraction() {
        return `   
        `
    }
}